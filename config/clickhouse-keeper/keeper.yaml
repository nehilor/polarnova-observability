# PolarNova ClickHouse Keeper — adapted from SigNoz Foundry defaults
keeper_server:
  coordination_settings:
    force_sync: false
    operation_timeout_ms: 10000
    raft_logs_level: warning
    session_timeout_ms: 30000
    snapshot_distance: 100000
    snapshots_to_keep: 3
  four_letter_word_white_list: '*'
  log_storage_path: /var/lib/clickhouse-keeper/coordination/log
  raft_configuration:
    server:
      - hostname: clickhouse-keeper
        id: 0
        port: 9234
  server_id: 0
  snapshot_storage_path: /var/lib/clickhouse-keeper/coordination/snapshots
  tcp_port: 9181
# Keeper serves on IPv4; ClickHouse is forced to resolve to IPv4
# (dns_allow_resolve_names_to_ipv4_addresses) so it connects here.
listen_host: 0.0.0.0
logger:
  console: true
  level: information
